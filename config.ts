// Configuration file to easily switch between demo and access-expired states
export const APP_CONFIG = {
  // Toggle this to switch between states:
  // true = Show "Access Expired" page
  // false = Show the original demo
  ACCESS_EXPIRED: true,
};